- To get started, you will need to install libraies nba_api.pybaseball,ntlk,spacy,clingo and pandas.

- You will also need to run pip install markupsafe==2.0.1. There will be an error that will say you need to install a version of a different module. You will need to install that as well.

- You can install all of these files by using pip.

- You can run the NBA_query analyzer which have queries done

- There are queries for the MLB query analyzer in the sample questions.

-There is a document that has a queries you can run in it and you can modify them

- The test cases will just run all the functions and tell you how long they took